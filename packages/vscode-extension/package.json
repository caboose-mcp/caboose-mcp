{
  "name": "vscode-fafb",
  "displayName": "Caboose MCP",
  "description": "VS Code sidebar and status bar for the fafb personal AI toolserver",
  "version": "0.1.0",
  "publisher": "caboose",
  "repository": {
    "type": "git",
    "url": "https://github.com/fafb/vscode-extension"
  },
  "engines": { "vscode": "^1.85.0" },
  "categories": ["Other"],
  "activationEvents": ["onStartupFinished"],
  "main": "./out/extension.js",
  "contributes": {
    "configuration": {
      "title": "Caboose MCP",
      "properties": {
        "cabooseMcp.transport": {
          "type": "string",
          "enum": ["http", "stdio"],
          "default": "http",
          "description": "Transport used to connect to fafb. Use 'http' (default) for a running server, or 'stdio' to spawn the binary directly."
        },
        "cabooseMcp.host": {
          "type": "string",
          "default": "localhost",
          "description": "Hostname of the fafb HTTP server (http transport only)."
        },
        "cabooseMcp.port": {
          "type": "number",
          "default": 3000,
          "description": "Port of the fafb HTTP server (http transport only)."
        },
        "cabooseMcp.binaryPath": {
          "type": "string",
          "default": "",
          "description": "Absolute path to the fafb binary (e.g. /home/caboose/dev/fafb/fafb)."
        },
        "cabooseMcp.env": {
          "type": "object",
          "default": {},
          "description": "Environment variables passed to the fafb process (e.g. SLACK_TOKEN, DISCORD_TOKEN, GPG_KEY_ID).",
          "additionalProperties": { "type": "string" }
        },
        "cabooseMcp.enabledTools": {
          "type": "array",
          "items": { "type": "string" },
          "default": ["*"],
          "description": "Tools to show in the sidebar. Use [\"*\"] for all, or list specific names like [\"focus_start\", \"note_add\"]."
        },
        "cabooseMcp.autoConnect": {
          "type": "boolean",
          "default": true,
          "description": "Automatically connect to fafb on VS Code startup if binaryPath is set."
        },
        "cabooseMcp.statusBar.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Show a status bar item for fafb."
        },
        "cabooseMcp.statusBar.tool": {
          "type": "string",
          "default": "focus_status",
          "description": "Tool to poll for status bar display. Leave empty to show only connection status."
        },
        "cabooseMcp.statusBar.intervalSeconds": {
          "type": "number",
          "default": 30,
          "minimum": 5,
          "description": "How often (in seconds) to poll the status bar tool."
        }
      }
    },
    "viewsContainers": {
      "activitybar": [
        {
          "id": "fafb",
          "title": "Caboose MCP",
          "icon": "media/icon.svg"
        }
      ]
    },
    "views": {
      "fafb": [
        {
          "id": "cabooseMcp.tools",
          "name": "Tools",
          "contextualTitle": "Caboose MCP Tools"
        }
      ]
    },
    "commands": [
      {
        "command": "cabooseMcp.connect",
        "title": "Connect",
        "category": "Caboose MCP",
        "icon": "$(plug)"
      },
      {
        "command": "cabooseMcp.disconnect",
        "title": "Disconnect",
        "category": "Caboose MCP",
        "icon": "$(debug-disconnect)"
      },
      {
        "command": "cabooseMcp.refresh",
        "title": "Refresh Tools",
        "category": "Caboose MCP",
        "icon": "$(refresh)"
      },
      {
        "command": "cabooseMcp.runTool",
        "title": "Run Tool",
        "category": "Caboose MCP",
        "icon": "$(run)"
      },
      {
        "command": "cabooseMcp.openSettings",
        "title": "Open Settings",
        "category": "Caboose MCP",
        "icon": "$(gear)"
      }
    ],
    "menus": {
      "view/title": [
        {
          "command": "cabooseMcp.refresh",
          "when": "view == cabooseMcp.tools",
          "group": "navigation"
        },
        {
          "command": "cabooseMcp.connect",
          "when": "view == cabooseMcp.tools && !cabooseMcp.connected",
          "group": "navigation"
        },
        {
          "command": "cabooseMcp.disconnect",
          "when": "view == cabooseMcp.tools && cabooseMcp.connected",
          "group": "navigation"
        },
        {
          "command": "cabooseMcp.openSettings",
          "when": "view == cabooseMcp.tools",
          "group": "navigation"
        }
      ],
      "view/item/context": [
        {
          "command": "cabooseMcp.runTool",
          "when": "view == cabooseMcp.tools && viewItem == tool",
          "group": "inline"
        }
      ]
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "package": "vsce package",
    "lint": "eslint src --ext ts"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/vscode": "^1.85.0",
    "@vscode/vsce": "^2.22.0",
    "typescript": "^5.3.0"
  }
}
