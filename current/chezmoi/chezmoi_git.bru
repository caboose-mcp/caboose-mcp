meta {
  name: chezmoi_git
  type: http
  seq: 30
}

post {
  url: {{baseUrl}}/mcp
  body: json
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

body:json {
  {
    "jsonrpc": "2.0",
    "id": 30,
    "method": "tools/call",
    "params": {
      "name": "chezmoi_git",
      "arguments": {}
    }
  }
}

docs {
  Run a git command inside the chezmoi source directory (e.g. to commit dotfile changes or check remote status).
}
