meta {
  name: tool_scaffold
  type: http
  seq: 117
}

post {
  url: {{baseUrl}}/mcp
  body: json
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

body:json {
  {
    "jsonrpc": "2.0",
    "id": 117,
    "method": "tools/call",
    "params": {
      "name": "tool_scaffold",
      "arguments": {}
    }
  }
}

docs {
  Generate boilerplate Go source code for a new MCP tool. Returns source you can review, edit, then pass to tool_write.
}
