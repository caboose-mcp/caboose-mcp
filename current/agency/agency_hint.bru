meta {
  name: agency_hint
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/mcp
  body: json
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

body:json {
  {
    "jsonrpc": "2.0",
    "id": 2,
    "method": "tools/call",
    "params": {
      "name": "agency_hint",
      "arguments": {}
    }
  }
}

docs {
  Return a formatted context hint block for a message. Paste this into Claude's context to get context-aware tool preferences.
}
