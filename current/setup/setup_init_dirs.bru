meta {
  name: setup_init_dirs
  type: http
  seq: 94
}

post {
  url: {{baseUrl}}/mcp
  body: json
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

body:json {
  {
    "jsonrpc": "2.0",
    "id": 94,
    "method": "tools/call",
    "params": {
      "name": "setup_init_dirs",
      "arguments": {}
    }
  }
}

docs {
  Initialize required CLAUDE_DIR subdirectories (secrets/, pending/, errors/, learning/).
}
