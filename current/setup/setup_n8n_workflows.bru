meta {
  name: setup_n8n_workflows
  type: http
  seq: 95
}

post {
  url: {{baseUrl}}/mcp
  body: json
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

body:json {
  {
    "jsonrpc": "2.0",
    "id": 95,
    "method": "tools/call",
    "params": {
      "name": "setup_n8n_workflows",
      "arguments": {}
    }
  }
}

docs {
  Return example n8n workflow JSON for scheduling recurring caboose-mcp tasks (tech digest, learning sessions, self-scan).
}
