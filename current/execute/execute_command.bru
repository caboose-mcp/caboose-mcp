meta {
  name: execute_command
  type: http
  seq: 57
}

post {
  url: {{baseUrl}}/mcp
  body: json
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

body:json {
  {
    "jsonrpc": "2.0",
    "id": 57,
    "method": "tools/call",
    "params": {
      "name": "execute_command",
      "arguments": {}
    }
  }
}

docs {
  Execute a shell command and return stdout+stderr.
}
