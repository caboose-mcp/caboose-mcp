meta {
  name: docker_list_containers
  type: http
  seq: 53
}

post {
  url: {{baseUrl}}/mcp
  body: json
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

body:json {
  {
    "jsonrpc": "2.0",
    "id": 53,
    "method": "tools/call",
    "params": {
      "name": "docker_list_containers",
      "arguments": {}
    }
  }
}

docs {
  List Docker containers (running by default, all with all=true).
}
