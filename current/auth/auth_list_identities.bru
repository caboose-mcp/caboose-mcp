meta {
  name: auth_list_identities
  type: http
  seq: 11
}

post {
  url: {{baseUrl}}/mcp
  body: json
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

body:json {
  {
    "jsonrpc": "2.0",
    "id": 11,
    "method": "tools/call",
    "params": {
      "name": "auth_list_identities",
      "arguments": {}
    }
  }
}

docs {
  List all identity → token mappings.
}
