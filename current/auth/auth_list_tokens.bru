meta {
  name: auth_list_tokens
  type: http
  seq: 12
}

post {
  url: {{baseUrl}}/mcp
  body: json
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

body:json {
  {
    "jsonrpc": "2.0",
    "id": 12,
    "method": "tools/call",
    "params": {
      "name": "auth_list_tokens",
      "arguments": {}
    }
  }
}

docs {
  List all non-revoked tokens with label, allowed tools, and expiry.
}
