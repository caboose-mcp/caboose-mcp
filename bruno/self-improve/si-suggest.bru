meta {
  name: si-suggest
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/mcp
  body: json
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

body:json {
  {
    "jsonrpc": "2.0",
    "id": 4,
    "method": "tools/call",
    "params": {
      "name": "si_suggest",
      "arguments": {
        ""title"": "Add error handling"
        ""description"": "Add better error handling to X"
        ""category"": "refactor"
        ""dir"": "/home/caboose/dev/fafb"
      }
    }
  }
}
